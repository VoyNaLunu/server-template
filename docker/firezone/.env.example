#DEFAULT, generated by firezone container
VERSION=0.7.32
EXTERNAL_URL=https://firezone.example.com/
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=
GUARDIAN_SECRET_KEY=
SECRET_KEY_BASE=
LIVE_VIEW_SIGNING_SALT=
COOKIE_SIGNING_SALT=
COOKIE_ENCRYPTION_SALT=
DATABASE_ENCRYPTION_KEY=
DATABASE_PASSWORD=
# The ability to change the IPv4 and IPv6 address pool will be removed
# in a future Firezone release in order to reduce the possible combinations
# of network configurations we need to handle.
#
# Due to the above, we recommend not changing these unless absolutely
# necessary.
WIREGUARD_IPV4_NETWORK=100.64.0.0/10
WIREGUARD_IPV4_ADDRESS=100.64.0.1
WIREGUARD_IPV6_NETWORK=fd00::/106
WIREGUARD_IPV6_ADDRESS=fd00::1


#ADDITIONAL
WIREGUARD_PORT=18200
PHOENIX_EXTERNAL_TRUSTED_PROXIES=["172.25.0.2"]
OUTBOUND_EMAIL_FROM="firezone@example.com"
OUTBOUND_EMAIL_ADAPTER=Elixir.Swoosh.Adapters.SMTP
OUTBOUND_EMAIL_ADAPTER_OPTS={ "relay": "mail.example.com", "username": "firezone@example.com", "password": "P@ssw0rd", "ssl": false, "tls": "always", "auth": "always", "port": 587 }